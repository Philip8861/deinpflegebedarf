{% comment %}
  Warenkorb-Pop-up: „Oft zusammen gekauft“ — kompletter Block inkl. Überschrift.
  Produkte aus Kollektion oft-zusammen-gekauft; Artikel im Warenkorb werden übersprungen.
{% endcomment %}
{%- liquid
  assign reco_col = collections['oft-zusammen-gekauft']
  assign reco_limit = 4
  assign reco_count = 0
-%}
{%- if reco_col != blank and reco_col.all_products_count > 0 -%}
  {%- capture reco_items -%}
    {%- for reco_product in reco_col.products -%}
      {%- if reco_count >= reco_limit -%}
        {%- break -%}
      {%- endif -%}
      {%- assign in_cart = false -%}
      {%- for item in cart.items -%}
        {%- if item.product_id == reco_product.id -%}
          {%- assign in_cart = true -%}
          {%- break -%}
        {%- endif -%}
      {%- endfor -%}
      {%- unless in_cart -%}
        {%- assign reco_variant = reco_product.selected_or_first_available_variant -%}
        <li class="cart-notification__reco">
          <div class="cart-notification__reco-card">
            <a
              href="{{ reco_product.url }}"
              class="cart-notification__reco-link"
              aria-label="{{ reco_product.title | escape }} – Produktseite öffnen"
            >
              <span class="cart-notification__reco-media">
                {%- if reco_product.featured_image -%}
                  <img
                    src="{{ reco_product.featured_image | image_url: width: 360 }}"
                    srcset="{{ reco_product.featured_image | image_url: width: 240 }} 240w, {{ reco_product.featured_image | image_url: width: 360 }} 360w, {{ reco_product.featured_image | image_url: width: 480 }} 480w"
                    sizes="(min-width: 990px) 18vw, (min-width: 750px) 22vw, 42vw"
                    alt="{{ reco_product.featured_image.alt | default: reco_product.title | escape }}"
                    width="180"
                    height="180"
                    loading="lazy"
                  >
                {%- else -%}
                  <span class="cart-notification__reco-placeholder" aria-hidden="true">
                    {{- 'product-1' | placeholder_svg_tag: 'placeholder-svg' -}}
                  </span>
                {%- endif -%}
              </span>
              <span class="cart-notification__reco-name">{{ reco_product.title | escape }}</span>
              <span class="cart-notification__reco-price">
                {%- if reco_product.price_varies -%}
                  ab {{ reco_product.price_min | money }}
                {%- else -%}
                  {{ reco_product.price | money }}
                {%- endif -%}
              </span>
            </a>
            {%- if reco_product.available and reco_product.variants.size <= 1 and reco_variant -%}
              <button
                type="button"
                class="pflege-cat-card__cart cart-notification__reco-cart"
                data-variant-id="{{ reco_variant.id }}"
                aria-label="{{ 'products.product.add_to_cart' | t }} — {{ reco_product.title | escape }}"
              >
                <span class="pflege-cat-card__cart-icon" aria-hidden="true">
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
                </span>
                <span>{{ 'products.product.add_to_cart' | t }}</span>
              </button>
            {%- else -%}
              <a
                href="{{ reco_product.url }}"
                class="pflege-cat-card__cart pflege-cat-card__cart--link cart-notification__reco-cart"
                aria-label="{{ reco_product.title | escape }} — {% if reco_product.available %}{{ 'products.product.choose_options' | t }}{% else %}{{ 'products.product.sold_out' | t }}{% endif %}"
              >
                <span class="pflege-cat-card__cart-icon" aria-hidden="true">
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
                </span>
                <span>{%- if reco_product.available -%}{{ 'products.product.choose_options' | t }}{%- else -%}{{ 'products.product.sold_out' | t }}{%- endif -%}</span>
              </a>
            {%- endif -%}
          </div>
        </li>
        {%- assign reco_count = reco_count | plus: 1 -%}
      {%- endunless -%}
    {%- endfor -%}
  {%- endcapture -%}
  {%- assign reco_items = reco_items | strip -%}
  {%- if reco_items != blank -%}
    <section class="cart-notification__recos" aria-labelledby="cart-notification-recos-title">
      <div class="cart-notification__recos-heading-wrap">
        <h3 id="cart-notification-recos-title" class="cart-notification__recos-heading">
          {{ 'general.cart.notification_recos_heading' | t }}
        </h3>
      </div>
      <ul class="cart-notification__recos-grid list-unstyled" role="list">
        {{ reco_items }}
      </ul>
    </section>
  {%- endif -%}
{%- endif -%}
