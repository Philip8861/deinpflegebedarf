{{ 'section-pflege-product-finder.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign primary_href = section.settings.primary_link | default: routes.all_products_collection_url
  assign secondary_href = section.settings.secondary_link
  assign show_secondary = false
  if section.settings.secondary_label != blank and secondary_href != blank
    assign show_secondary = true
  endif

  assign pf_finder_bg_url = 'produktfinder.webp' | asset_url
  if section.settings.background_image != blank
    assign pf_finder_bg_url = section.settings.background_image | image_url: width: 2400
  endif
-%}

<section class="pflege-product-finder" {{ section.shopify_attributes }}>
  <div class="pflege-product-finder__backdrop">
    <div class="pflege-product-finder__shell">
      {%- if section.settings.heading != blank -%}
        <h2 class="pflege-product-finder__section-title">{{ section.settings.heading | escape }}</h2>
      {%- endif -%}
      <div
        class="pflege-product-finder__panel"
        style="--pflege-product-finder-bg: url('{{ pf_finder_bg_url }}');"
      >
        <div class="pflege-product-finder__inner">
        <div class="pflege-product-finder__stack">
          <div class="pflege-product-finder__lead">
            {%- if section.settings.intro != blank -%}
              <p class="pflege-product-finder__intro">{{ section.settings.intro | escape }}</p>
            {%- endif -%}
          </div>

          <div class="pflege-product-finder__actions pflege-product-finder__actions--center">
            {%- if section.settings.primary_label != blank -%}
              <a class="pflege-product-finder__cta" href="{{ primary_href }}">
                {{- section.settings.primary_label | escape -}}
                <svg width="26" height="26" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            {%- endif -%}
            {%- if show_secondary -%}
              <a class="pflege-product-finder__link-secondary" href="{{ secondary_href }}">
                {{- section.settings.secondary_label | escape -}}
              </a>
            {%- endif -%}
          </div>

        </div>
      </div>
    </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "PflegeShop Produktfinder",
  "tag": "section",
  "class": "pflege-product-finder-section",
  "settings": [
    {
      "type": "paragraph",
      "content": "Hintergrundbild im Kasten: unten wählen oder leer lassen für Standard (Theme-Asset produktfinder.webp). Empfohlen: breites JPG/WebP (ca. 1900–2400 px), dezente Farben."
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Hintergrundbild Produktfinder-Kasten"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Hauptüberschrift",
      "default": "Nutzen Sie unseren Produktfinder"
    },
    {
      "type": "textarea",
      "id": "intro",
      "label": "Text im Kasten (über dem Button)",
      "default": "Kein langes Suchen: Mit wenigen Klicks finden Sie passende Pflegeprodukte für Ihren individuellen Bedarf. Ob Hautpflege, Inkontinenz, Hygiene, Desinfektion, Schutz oder Pflegehilfen, unser Produktfinder führt Sie einfach durch die wichtigsten Fragen und zeigt Ihnen schnell passende Empfehlungen für Ihre persönliche Pflegesituation."
    },
    {
      "type": "text",
      "id": "primary_label",
      "label": "Primär-Button — Text",
      "default": "Jetzt das passende Produkt finden"
    },
    {
      "type": "url",
      "id": "primary_link",
      "label": "Primär-Button — Link",
      "info": "Leer = Link zur Gesamt­kollektion."
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "Sekundär — Text",
      "default": "Mehr erfahren"
    },
    {
      "type": "url",
      "id": "secondary_link",
      "label": "Sekundär — Link",
      "info": "Ohne Link ausgefüllt wird der Textlink ausgeblendet."
    }
  ],
  "presets": [
    {
      "name": "PflegeShop Produktfinder"
    }
  ]
}
{% endschema %}
